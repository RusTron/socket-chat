export enum HeadingType {
  h1 = 'h1',
  h2 = 'h2',
  h3 = 'h3',
  h4 = 'h4',
  h5 = 'h5',
  h6 = 'h6',
  p = 'p'
}

export enum Log {
  in = 'Log in',
  out = 'Log out'
}

export enum ActionTypes {
  SET_OUR_NAME,
  SET_NEW_MESSAGE,
  SET_FIRST_MESSAGE,
  SET_TYPING,
  SET_STOP_TYPING,
 }

export enum ButtonType {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}

export enum FlexDirectionTypes {
  column = 'column',
  row = 'row',
}
